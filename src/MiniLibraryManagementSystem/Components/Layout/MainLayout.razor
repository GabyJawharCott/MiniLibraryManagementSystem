@inherits LayoutComponentBase
@using Microsoft.AspNetCore.Components.Authorization

<div class="page">
    <div class="sidebar" id="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4 d-flex align-items-center justify-content-between">
            <AuthorizeView>
                <Authorized>
                    <span class="text-muted small">Signed in as <strong>@(GetUserDisplayName(context.User))</strong></span>
                </Authorized>
            </AuthorizeView>
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank" class="ms-auto">About</a>
        </div>

        <article class="content px-4">
            <ErrorBoundary>
                <ChildContent>@Body</ChildContent>
                <ErrorContent>
                    <div class="alert alert-danger p-4">
                        <strong>Something went wrong loading this page.</strong>
                        <p class="mb-0 mt-2"><a href="/dashboard" class="alert-link">Go to Dashboard</a> or <a href="." class="alert-link">Reload</a>.</p>
                    </div>
                </ErrorContent>
            </ErrorBoundary>
        </article>
    </main>
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>

@code {
    private static string GetUserDisplayName(System.Security.Claims.ClaimsPrincipal user)
    {
        var name = user?.Identity?.Name;
        if (!string.IsNullOrWhiteSpace(name)) return name;
        var email = user?.FindFirst(System.Security.Claims.ClaimTypes.Email)?.Value
            ?? user?.FindFirst("email")?.Value;
        return !string.IsNullOrWhiteSpace(email) ? email : "User";
    }
}

